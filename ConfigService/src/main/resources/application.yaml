server:
  port: 8888
spring:
  application:
    name: ConfigService
  cloud:
    config:
      server:
        git:
          uri: https://github.com/ashish-panicker/books_author_microservices-config
          clone-on-start: true
logging:
  pattern:
    console: "%msg%n"